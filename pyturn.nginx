# nginx configuration for websockets

server {
  server_name ~"^uwsgi-(\w+)\.myturn\..*$";
  access_log /var/log/nginx/pyturn-$1-access.log;
  # can get lots of vebosity by appending "debug" just before semicolon below
  error_log /var/log/nginx/pyturn-$1-error.log debug;
  rewrite_log on;
  # no index, wsgi handles "/"
  location / {
    root /usr/local/jcomeauictx/pyturn-$1/html;
    try_files $uri @proxy;
    proxy_redirect off;
  }
  location ~ /\.ht {
    deny all;
  }
  location ~ \.py.?$ {
    deny all;
  }
  location @proxy {
    include uwsgi_params;
    uwsgi_pass localhost:5678;
  }
}
